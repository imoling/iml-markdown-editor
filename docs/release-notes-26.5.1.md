## 26.5.1 — 界面上的字，少说一半

这一版没有新功能，只做了一件事：把界面上的说明文字重写了一遍。

把所有用户看得见的中文抓出来逐条读完之后，发现问题不是「字多」，而是**界面在替你做阅读理解**——
每个开关下面都挂着一段解释，讲的是「这是什么、我为什么这么设计、顺便我保证不干坏事」。
成熟软件的小字只回答一件事：**不做会怎样**。

### 下载

| 平台 | 安装包 |
|---|---|
| macOS Apple Silicon（M 系列） | `iML.Markdown.Editor-26.5.1-arm64.dmg` |
| macOS Intel | `iML.Markdown.Editor-26.5.1-x64.dmg` |
| Windows（绝大多数电脑选这个） | `iML.Markdown.Editor-Setup-26.5.1-x64.exe` |
| Windows on ARM（骁龙本等） | `iML.Markdown.Editor-Setup-26.5.1-arm64.exe` |

从 26.5.0 升上来的，功能和数据都不变，直接装即可。

### 改了什么

- **设置最底下那块写错了**：「安全逃生舱 / 销毁本地记忆快照 / 一键抹除全部现场」，还提到了这个应用里根本不存在的「星标状态与记忆目录」。
  它实际做的事很朴素——关掉所有标签页、清空最近打开和星标，笔记和设置一个字节都不动。现在就这么写：**重置界面状态**
- 同一页里的「知识库」「起始控制台」「静默自动保存」「恢复上次会话」，改成**笔记库 / 首页 / 自动保存 / 上次打开的笔记**
- **三个最常打开的弹窗瘦身**：本机资源的副标题原本用 60 个字讲内存调度策略，现在一句话；
  实时转写里那条 71 个字、套着括号和分号的录音说明，缩成「录音存在笔记旁的 assets 里，一小时约 11 MB」；
  「生图时总是先腾地方」这个开关不再一边勾着、一边写「默认不开」
- **出错时说人话**：以前配图失败会把服务端返回的原始内容前 200 个字糊在状态栏上；
  现在只说「火山引擎没有返回图片，换个模型或稍后再试」，技术细节留在日志里
- **标点统一**：省略号一律用「…」，中文句子里不再混半角冒号
- 一些前后不一致的叫法也统一了：本机 / 本地、笔记 / 文档、点一下 / 点击、设置里 / 设置中

界面说明文字从 5372 字减到 3139 字，少了四成，没有一个功能因此变得难懂。

### 顺带

为了防止文字「改完又长回来」，新增了一组自动检查跟着测试一起跑：省略号、半角冒号、
禁用词、术语一致性、说明文案长度上限、实现细节词、跨文件重复的句子。
