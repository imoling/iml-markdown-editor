/**
 * 主进程和界面都要说的那几句话。
 * 这两边不能互相 import 组件，但可以共用 shared/ 里的纯模块——同一件事就说同一句，
 * 别一边写「AI 功能已在设置中关闭」、另一边写「已在设置里关闭」。
 */

/** AI 总开关关着时，任何 AI 入口给的同一句话 */
export const AI_DISABLED = 'AI 功能已在设置里关闭';

/** 麦克风没授权：录音、转写、试音三条路都会撞上 */
export const MIC_DENIED = '没有麦克风权限，到系统设置里允许一下';
